import SectionContainer from '../SectionContainer';
import SectionHeader from '../SectionHeader';

export function RSVPSection() {
  return (
    <SectionContainer className="bg-gray-200">
      <SectionHeader>RSVP i kontakt</SectionHeader>
    </SectionContainer>
  );
}
